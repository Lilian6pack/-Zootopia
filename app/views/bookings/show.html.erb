<div class="container text-center mt-5">
  <h1> Your booking details </h1>

  <div class="row">
        <div class="col-12 col-md-6 col-lg-6">
          <div class="card text-center mt-5">
            <%= cl_image_tag @booking.animal.photo_url.key, height: 300, width: 400, crop: :fill %>
            <div class="card-description">
              <h3> Your animal is <%= @booking.animal.name %></h3>
              <p>N° of your booking : <%= @booking.id %></p>
              <p>From <%= @booking.start_date %></p>
              <p>To <%= @booking.end_date %></p>
              <%= link_to "Go back to your bookings", animal_bookings_path, class: "btn btn-primary btn-lg" %>
            </div>
          </div>
        </div>

        <div class="col-12 col-md-6 col-lg-6">
          <div class="card text-center mt-5">
            <div class="card-description">
              <p> Daily price: <%= @booking.animal.daily_price %> Euros</p>
              <p> X </p>
              <%= pluralize(@nb_of_days, 'day') %>
              <p> = </p>
              <h3><strong><%= @total_price.to_s + " Euros" %></strong></h3><br>
              <%= link_to "Pay", "#", class: "btn btn-primary btn-lg px-5" %>
            </div>
          </div>
        </div>


  </div>
</div>
